{
  "name": "getsoel-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/bin.ts --target node --format esm --banner '#!/usr/bin/env node' --outfile skills/context-index/scripts/context-index.mjs",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test && bun run build && git diff --exit-code -- skills/context-index/scripts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
